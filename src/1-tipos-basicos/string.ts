let nome: string;
let sobrenome: string;

// ...

nome = 'Josan';
sobrenome = 'Pontes'

function nomeCompleto(nome: string,
sobrenome: string) {
  return `${nome} ${sobrenome}`
}

nomeCompleto('Josan', 'Pontes');